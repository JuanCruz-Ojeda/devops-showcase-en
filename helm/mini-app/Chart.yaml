apiVersion: v2
name: mini-app
description: Flask + Redis mini-service (DevOps technical test) — first-party chart
type: application

# Chart version (semver). Bumped on every chart change.
version: 0.1.0

# Version of the application being deployed (informational).
appVersion: "1.0.0"
