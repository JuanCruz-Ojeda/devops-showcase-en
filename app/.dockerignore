# Shrinks the build context and keeps junk out of the image.
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.ruff_cache/

# The Dockerfile itself is not needed inside the image.
Dockerfile
.dockerignore
