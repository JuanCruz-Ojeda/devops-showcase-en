# Copy this file to .env ONLY if you need to override the default values.
# docker-compose.yml already uses these same defaults, so the stack comes up without a .env.
#
# Inside the compose network, Redis is reachable by its service name.
REDIS_HOST=redis
REDIS_PORT=6379
